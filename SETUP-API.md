# Instagram 공식 API 셋업 가이드 (계정당 1회, 약 10분)

웹 로그인 자동화의 캡차/봇감지 문제를 원천 차단하는 **공식 경로**.
Meta가 허용하는 방식이라 계정 안전성도 훨씬 높다.

---

## 1단계. 인스타 계정을 프로페셔널로 전환 (폰에서, 2분, 무료)

1. 인스타그램 앱 → 프로필 → 우상단 ☰ → **설정 및 개인정보**
2. **계정 유형 및 도구** → **프로페셔널 계정으로 전환**
3. **크리에이터** 선택 (카테고리 아무거나) → 완료

> 겉보기 변화 거의 없음. 프로필에 카테고리 표시만 생김 (숨기기 가능).

## 2단계. Meta 개발자 앱 만들기 (PC에서, 3분)

1. https://developers.facebook.com 접속 → 페이스북 계정으로 로그인
2. 우상단 **내 앱** → **앱 만들기**
3. 사용 사례: **"Instagram 관리"** (안 보이면 "기타" → 앱 유형 "비즈니스") 선택
4. 앱 이름: `InstaAuto` (아무거나) → **앱 만들기**

## 3단계. 인스타 계정 연결 + 토큰 발급 (5분)

> ⚠️ **먼저 테스터 역할 등록 필수** — 이거 안 하면 계정 연결 시
> "개발자 역할 권한 부족" 오류가 난다.

1. 앱 대시보드 → **앱 역할** → **역할** → **사람 추가** → **Instagram 테스터** 선택 → 인스타 사용자 이름 입력 → 추가
2. **인스타그램에서 초대 수락**: instagram.com 로그인 → 설정 → **앱 및 웹사이트** (`instagram.com/accounts/manage_access/`) → **테스터 초대** 탭 → **수락**
3. 앱 대시보드 → **이용 사례** → **맞춤 설정** → **Instagram 로그인이 포함된 API 설정** → **2. 액세스 토큰 생성** 섹션 → **계정 추가** → 팝업에서 **허용**
4. 계정이 목록에 뜨면 → **토큰 생성** 클릭 → 팝업 **허용** → **이해합니다** 체크 → **복사** (IGAA... 로 시작하는 긴 문자열)
5. (권한 확인) **권한 및 기능** 페이지에서 `instagram_business_basic` 과 `instagram_business_content_publish` 가 "테스트 준비 완료"인지 확인 — 아니면 각각 **추가** 클릭

## 4단계. 토큰 등록 (터미널, 30초)

복사한 토큰을 아래 명령의 `<토큰>` 자리에 붙여넣고 실행:

```bash
cd /Users/seungsoohan/Projects/InstaAuto
node scripts/setup-token.js bobi '<토큰>'
```

`✅ 토큰 유효! 계정: @bobiai2026` 이 뜨면 성공.

## 5단계. 테스트 발행

```bash
cd /Users/seungsoohan/Projects/InstaAuto
node scripts/run-once.js bobi
```

콘텐츠 생성 → API 발행 → 슬랙 보고까지 자동. 게시물 링크가 슬랙으로 온다.

---

## 참고

- **토큰 수명**: 60일. 파이프라인이 **7일마다 자동 갱신**하므로 계속 돌아가는 한 만료 안 됨.
  (2달 이상 맥이 꺼져 있었다면 만료 → 슬랙 🔑 알림 → 3단계 토큰 재발급 → 4단계 재등록)
- **계정 추가**: 같은 Meta 앱에서 계정만 더 추가 → 토큰 발급 → `setup-token.js <계정ID> '<토큰>'`
- **앱 검수 불필요**: 개발 모드로 충분 (본인 계정 발행은 검수 없이 가능)
- **발행 한도**: 24시간당 100개 (우리는 하루 2개 — 여유 충분)
- API 토큰이 등록된 계정은 자동으로 API 발행, 없는 계정은 웹 자동화로 폴백
